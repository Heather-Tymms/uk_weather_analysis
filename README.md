# uk_weather_analysis

This is a repository showing how to scrape and analyse data from the web.

## The data

We are using UK weather data from the Met Office that they publish and update regularly.

## The flow of running

In this repository, we pull information from

### Scrape the initial table

First we need to make sure the data is still in the correct place. Go to the [website](https://www.metoffice.gov.uk/research/climate/maps-and-data/historic-station-data) to ensure it exists before running any code.

Once the website has been found to exist, it is able to run the `main.py` file by ...

After running `main.py`, you will see the input folder in the folder structure. You should now be able to see a `csv` in there. This shows each of the stations with some attributes like the location and URL that holds the txt data online. For each of the locations you run going forward,

### Run the notebooks
